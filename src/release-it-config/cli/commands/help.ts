export default (): void => {
   // eslint-disable-next-line no-console
   console.log(`
   (silvermine-release) Allowed commands: release, pre-release --prefix={rc | alpha | beta}, tag, changelog {--write}, --help
   `);
};
